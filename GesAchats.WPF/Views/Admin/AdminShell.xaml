<Window x:Class="GesAchats.WPF.Views.Admin.AdminShell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrls="clr-namespace:GesAchats.WPF.Controls"
        xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
        xmlns:converters="clr-namespace:GesAchats.WPF.Converters"
        mc:Ignorable="d"
        Title="{Binding Title}"
        WindowState="Maximized"
        ResizeMode="CanResize"
        SizeToContent="Manual"
        WindowStartupLocation="CenterScreen"
        MinWidth="1100"
        MinHeight="700"
        FontFamily="Segoe UI">
    <Window.Resources>
        <converters:BoolToGridLengthConverter x:Key="BoolToGridLengthConverter" />
        <converters:BooleanInverterConverter x:Key="BooleanInverterConverter" />
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding IsCollapsed, Converter={StaticResource BoolToGridLengthConverter}}"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{StaticResource SidebarBackgroundBrush}" 
                Effect="{StaticResource CardShadow}">
            <DockPanel LastChildFill="True">
                <!-- Logo -->
                <StackPanel DockPanel.Dock="Top" Margin="16,16,16,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <Border Width="44" Height="44" CornerRadius="10" Background="#40FFFFFF" Margin="0,0,12,0">
                                <fa:IconImage Icon="Cube" Width="24" Height="24" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center" Visibility="{Binding IsCollapsed, Converter={StaticResource BooleanInverterConverter}}">
                                <TextBlock Text="GesAchats" Foreground="White" FontSize="20" FontWeight="Bold"/>
                            </StackPanel>
                        </StackPanel>
                        <Button Grid.Column="1" Padding="8,6" 
                                HorizontalAlignment="Right" 
                                Background="Transparent" 
                                BorderThickness="0" 
                                Cursor="Hand" 
                                Command="{Binding ToggleSidebarCommand}">
                            <fa:IconImage Icon="ChevronRight" Width="18" Height="18" Foreground="White"/>
                        </Button>
                    </Grid>
                </StackPanel>

                <!-- Profile & Logout -->
                <StackPanel DockPanel.Dock="Bottom" Margin="16,12,16,20">
                    <Border Background="{StaticResource SidebarGlassBackground}" 
                            CornerRadius="16" 
                            Padding="12,12"
                            BorderBrush="{StaticResource SidebarGlassBorder}"
                            BorderThickness="1">
                        <StackPanel>
                            <Grid HorizontalAlignment="Center" Margin="0,0,0,8">
                                <Border Width="56" Height="56" CornerRadius="28" Background="#80FFFFFF">
                                    <TextBlock Text="AD" 
                                               Foreground="#333333" 
                                               FontSize="22" 
                                               FontWeight="Bold" 
                                               HorizontalAlignment="Center" 
                                               VerticalAlignment="Center"/>
                                </Border>
                            </Grid>
                            <StackPanel Visibility="{Binding IsCollapsed, Converter={StaticResource BooleanInverterConverter}}">
                                <TextBlock Text="{Binding UserName}" 
                                           Foreground="White" 
                                           FontSize="15" 
                                           FontWeight="SemiBold" 
                                           TextAlignment="Center"/>
                                <TextBlock Text="Administrateur" 
                                           Foreground="#B8A9D4" 
                                           FontSize="12" 
                                           TextAlignment="Center"
                                           Opacity="0.9"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                    <Button Margin="0,12,0,0" Padding="12,8" 
                            HorizontalAlignment="Stretch" 
                            Cursor="Hand"
                            Command="{Binding LogoutCommand}"
                            FontSize="14"
                            FontWeight="SemiBold">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="LogoutBorder"
                                        Background="{StaticResource SidebarGlassBackground}" 
                                        BorderBrush="{StaticResource SidebarGlassBorder}"
                                        BorderThickness="1"
                                        CornerRadius="14"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="LogoutBorder" Property="Background" Value="#40FFFFFF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <fa:IconImage Grid.Column="0" Icon="SignOutAlt" Width="22" Height="22" Foreground="White" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="DÉCONNEXION" Foreground="White" Margin="12,0,0,0" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Visibility="{Binding IsCollapsed, Converter={StaticResource BooleanInverterConverter}}"/>
                        </Grid>
                    </Button>
                </StackPanel>

                <!-- Navigation (last child, fills remaining space) -->
                <ScrollViewer VerticalScrollBarVisibility="Hidden" 
                           HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="12,0,12,16">
                        <!-- Menu Section -->
                        <TextBlock Text="MENU" 
                                   Foreground="#B8A9D4" 
                                   FontSize="10" 
                                   FontWeight="Bold" 
                                   Margin="12,0,0,8"
                                   Opacity="0.6"
                                   Visibility="{Binding IsCollapsed, Converter={StaticResource BooleanInverterConverter}}"/>
                        
                        <ctrls:SidebarItem Icon="Home" 
                                           Text="Dashboard" 
                                           IsActive="{Binding ActivePage, Converter={StaticResource EqualityConverter}, ConverterParameter=Dashboard}"
                                           Command="{Binding NavigateCommand}" 
                                           CommandParameter="Dashboard"
                                           Margin="0,0,0,4"
                                           IsCollapsed="{Binding IsCollapsed}"/>
                        
                        <ctrls:SidebarItem Icon="BoxesStacked" 
                                           Text="Stock Global" 
                                           IsActive="{Binding ActivePage, Converter={StaticResource EqualityConverter}, ConverterParameter=StockGlobal}"
                                           Command="{Binding NavigateCommand}" 
                                           CommandParameter="StockGlobal"
                                           Margin="0,0,0,4"
                                           IsCollapsed="{Binding IsCollapsed}"/>
                        
                        <ctrls:SidebarItem Icon="ClipboardList" 
                                           Text="Historique Besoins" 
                                           IsActive="{Binding ActivePage, Converter={StaticResource EqualityConverter}, ConverterParameter=NeedsHistory}"
                                           Command="{Binding NavigateCommand}" 
                                           CommandParameter="NeedsHistory"
                                           Margin="0,0,0,4"
                                           IsCollapsed="{Binding IsCollapsed}"/>
                        
                        <ctrls:SidebarItem Icon="History" 
                                           Text="Historique des sorties" 
                                           IsActive="{Binding ActivePage, Converter={StaticResource EqualityConverter}, ConverterParameter=HistoriqueSorties}"
                                           Command="{Binding NavigateCommand}" 
                                           CommandParameter="HistoriqueSorties"
                                           Margin="0,0,0,4"
                                           IsCollapsed="{Binding IsCollapsed}"/>
                        
                        <ctrls:SidebarItem Icon="FileContract" 
                                           Text="Bons de Livraison" 
                                           IsActive="{Binding ActivePage, Converter={StaticResource EqualityConverter}, ConverterParameter=DeliveryNotes}"
                                           Command="{Binding NavigateCommand}" 
                                           CommandParameter="DeliveryNotes"
                                           Margin="0,0,0,4"
                                           IsCollapsed="{Binding IsCollapsed}"/>
                        
                        <ctrls:SidebarItem Icon="MoneyBill" 
                                           Text="Prix &amp; Achats" 
                                           IsActive="{Binding ActivePage, Converter={StaticResource EqualityConverter}, ConverterParameter=PriceAnalysis}"
                                           Command="{Binding NavigateCommand}" 
                                           CommandParameter="PriceAnalysis"
                                           Margin="0,0,0,4"
                                           IsCollapsed="{Binding IsCollapsed}"/>
                        
                        <ctrls:SidebarItem Icon="CartShopping" 
                                           Text="Gestion Bons Commande" 
                                           IsActive="{Binding ActivePage, Converter={StaticResource EqualityConverter}, ConverterParameter=Orders}"
                                           Command="{Binding NavigateCommand}" 
                                           CommandParameter="Orders"
                                           Margin="0,0,0,4"
                                           IsCollapsed="{Binding IsCollapsed}"/>
                        
                        <ctrls:SidebarItem Icon="FileInvoice" 
                                           Text="Factures Fournisseurs" 
                                           IsActive="{Binding ActivePage, Converter={StaticResource EqualityConverter}, ConverterParameter=Invoices}"
                                           Command="{Binding NavigateCommand}" 
                                           CommandParameter="Invoices"
                                           Margin="0,0,0,4"
                                           IsCollapsed="{Binding IsCollapsed}"/>
                        
                        <ctrls:SidebarItem Icon="CreditCard" 
                                           Text="Suivi Règlements" 
                                           IsActive="{Binding ActivePage, Converter={StaticResource EqualityConverter}, ConverterParameter=Payments}"
                                           Command="{Binding NavigateCommand}" 
                                           CommandParameter="Payments"
                                           Margin="0,0,0,4"
                                           IsCollapsed="{Binding IsCollapsed}"/>
                        
                        <ctrls:SidebarItem Icon="Users" 
                                           Text="Gestion Employés" 
                                           IsActive="{Binding ActivePage, Converter={StaticResource EqualityConverter}, ConverterParameter=Employees}"
                                           Command="{Binding NavigateCommand}" 
                                           CommandParameter="Employees"
                                           Margin="0,0,0,4"
                                           IsCollapsed="{Binding IsCollapsed}"/>
                        
                        <ctrls:SidebarItem Icon="FolderOpen" 
                                           Text="Gestion des Factures" 
                                           IsActive="{Binding ActivePage, Converter={StaticResource EqualityConverter}, ConverterParameter=InvoicesAdmin}"
                                           Command="{Binding NavigateCommand}" 
                                           CommandParameter="InvoicesAdmin"
                                           Margin="0,0,0,4"
                                           IsCollapsed="{Binding IsCollapsed}"/>
                        <ctrls:SidebarItem Icon="UserFriends" 
                                           Text="Suivi Fournisseurs" 
                                           IsActive="{Binding ActivePage, Converter={StaticResource EqualityConverter}, ConverterParameter=SuiviFournisseurs}"
                                           Command="{Binding NavigateCommand}" 
                                           CommandParameter="SuiviFournisseurs"
                                           Margin="0,0,0,4"
                                           IsCollapsed="{Binding IsCollapsed}"/>
                    </StackPanel>
                </ScrollViewer>
            </DockPanel>
        </Border>

        <!-- Content Area -->
        <Frame x:Name="MainFrame" Grid.Column="1" Background="{StaticResource PageBackgroundBrush}" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
