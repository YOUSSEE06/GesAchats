<Window x:Class="GesAchats.WPF.Views.Auth.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="650" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    
    <Grid>
        <Border Margin="50" Background="{StaticResource SurfaceBrush}" CornerRadius="20" 
                MaxWidth="450" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" Direction="-90" RenderingBias="Quality" ShadowDepth="5" Opacity="0.15"/>
            </Border.Effect>
            
            <StackPanel Margin="40">
                <TextBlock Text="GesAchats" FontSize="42" FontWeight="Bold" 
                           Foreground="{StaticResource PrimaryBrush}" 
                           HorizontalAlignment="Center" Margin="0,0,0,5"/>
                
                <TextBlock Text="AUTHENTIFICATION" FontSize="14" FontWeight="SemiBold"
                           Foreground="{StaticResource TextSecondaryBrush}" 
                           HorizontalAlignment="Center" Margin="0,0,0,40"/>

                <StackPanel Margin="0,0,0,25">
                    <TextBlock Text="Identifiant" Margin="0,0,0,10" FontWeight="SemiBold" FontSize="14" Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                             Padding="15,12" FontSize="16" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}"/>
                </StackPanel>

                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="Mot de passe" Margin="0,0,0,10" FontWeight="SemiBold" FontSize="14" Foreground="{StaticResource TextPrimaryBrush}"/>
                    <PasswordBox x:Name="TxtPassword" Padding="15,12" FontSize="16" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}"/>
                </StackPanel>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" 
                           TextWrapping="Wrap" Margin="0,10,0,20" MinHeight="40" FontSize="14"
                           HorizontalAlignment="Center" TextAlignment="Center" FontWeight="Medium"/>

                <Button Content="SE CONNECTER" 
                        Command="{Binding LoginCommand}" 
                        CommandParameter="{Binding ElementName=TxtPassword}"
                        Background="{StaticResource PrimaryBrush}" 
                        Foreground="White" FontWeight="Bold" 
                        Padding="20" FontSize="16" Cursor="Hand"
                        IsDefault="True">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.9"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                
                <ProgressBar IsIndeterminate="True" Height="4" Margin="0,30,0,0"
                             Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                             Foreground="{StaticResource PrimaryBrush}" Background="Transparent" BorderThickness="0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
