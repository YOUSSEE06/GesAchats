<Window x:Class="GesAchats.WPF.Views.Components.SuccessModalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:GesAchats.WPF.Converters"
        mc:Ignorable="d"
        Title="Succès"
        Height="320"
        Width="420"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../Resources/Shared/CommonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <conv:PluralConverter x:Key="PluralConverter"/>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <!-- Modal Card -->
        <Border Width="400"
                Height="280"
                Background="White"
                CornerRadius="12"
                Padding="32"
                Effect="{StaticResource CardShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,24">
                    <Border Width="52"
                            Height="52"
                            Background="#E8F5E9"
                            CornerRadius="16">
                        <TextBlock Text="✓"
                                   FontSize="28"
                                   Foreground="#4CAF50"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontWeight="SemiBold"/>
                    </Border>
                </StackPanel>

                <!-- Body -->
                <StackPanel Grid.Row="1" HorizontalAlignment="Center">
                    <TextBlock Text="Succès !"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="#1F2937"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,8"/>
                    <TextBlock FontSize="14"
                               Foreground="#6B7280"
                               HorizontalAlignment="Center"
                               TextWrapping="Wrap">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="{}{0} demande{1} de devis a{1} été générée{1} avec succès.">
                                <Binding Path="Count" RelativeSource="{RelativeSource AncestorType=Window}"/>
                                <Binding Path="Count" RelativeSource="{RelativeSource AncestorType=Window}" ConverterParameter="s" Converter="{StaticResource PluralConverter}"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </StackPanel>

                <!-- Buttons -->
                <Grid Grid.Row="2" Margin="0,24,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Grid.Column="0"
                            Content="Voir la liste"
                            Padding="16,12"
                            Background="Transparent"
                            Foreground="#4F46E5"
                            FontWeight="SemiBold"
                            FontSize="14"
                            BorderBrush="#E5E7EB"
                            BorderThickness="2"
                            Click="ViewListButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="10"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#F9FAFB"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    
                    <Button Grid.Column="2"
                            Content="Créer un devis"
                            Padding="16,12"
                            Foreground="White"
                            FontWeight="SemiBold"
                            FontSize="14"
                            BorderThickness="0"
                            Click="CreateNewButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="10"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <Button.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#6366F1" Offset="0"/>
                                <GradientStop Color="#4F46E5" Offset="1"/>
                            </LinearGradientBrush>
                        </Button.Background>
                    </Button>
                </Grid>

                <!-- Close Button -->
                <Button HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Width="28"
                        Height="28"
                        Background="Transparent"
                        Foreground="#9CA3AF"
                        BorderThickness="0"
                        Padding="0"
                        FontSize="18"
                        Click="CloseButton_Click"
                        Margin="0,-16,-16,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="14"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <TextBlock Text="✕"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"/>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>
