<UserControl x:Class="GesAchats.WPF.Controls.KpiCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
             mc:Ignorable="d"
             d:DesignHeight="145" d:DesignWidth="200">
    <Border x:Name="MainBorder"
            Background="{StaticResource CardBackgroundBrush}"
            CornerRadius="14"
            Padding="18"
            BorderBrush="#E5E7EB"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" Opacity="0.05" ShadowDepth="2" Direction="270" Color="#000000"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Top Row: Title + Icon -->
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="TitleControl" 
                           Grid.Column="0" 
                           FontSize="13" 
                           Foreground="#64748B"
                           TextTrimming="CharacterEllipsis"
                           VerticalAlignment="Center"/>
                
                <!-- Icon Badge -->
                <Border x:Name="IconBadge"
                        Grid.Column="1"
                        Width="36"
                        Height="36"
                        CornerRadius="9"
                        VerticalAlignment="Center">
                    <fa:IconImage x:Name="IconControl"
                                  Width="16"
                                  Height="16"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Center"/>
                </Border>
            </Grid>
            
            <!-- Value -->
            <TextBlock x:Name="ValueControl"
                       Grid.Row="1"
                       FontSize="30"
                       FontWeight="Bold"
                       Foreground="#0F172A"
                       Margin="0,0,0,6"/>
            
            <!-- Unit + Trend -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="UnitControl"
                           Grid.Column="0"
                           FontSize="13"
                           Foreground="#64748B"
                           VerticalAlignment="Center"/>
                
                <StackPanel x:Name="TrendPanel"
                            Grid.Column="1"
                            Orientation="Horizontal"
                            Margin="8,0,0,0"
                            VerticalAlignment="Center">
                    <TextBlock x:Name="TrendIconControl"
                               FontSize="12"
                               FontWeight="Medium"
                               VerticalAlignment="Center"/>
                    <TextBlock x:Name="TrendTextControl"
                               FontSize="12"
                               FontWeight="Medium"
                               VerticalAlignment="Center"
                               Margin="2,0,0,0"
                               TextTrimming="CharacterEllipsis"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
